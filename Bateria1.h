#include <iostream>
#include <math.h>

using namespace std;
void Acumulador(long int byte){
	float bit = 4;
	if(byte != 0)
	{
		cout<<"| \Bytes : ";
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				bit++;
			}
			//cout<<"\n";
			cout<<byte;
		}
		//	return byte;
	}
	if(byte != 0)
	{
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				bit++;
			}
			cout<<byte;
			//cout<<"\n";
		}
		//	return byte;
	}
	if(byte != 0)
	{
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				bit++;
			}
			cout<<byte;
			//cout<<"\n";
		}
		//	return byte;
	}
		if(byte != 0)
	{
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				bit++;
			}
			cout<<byte;
			//cout<<"\n";
		}
		//	return byte;
	}
		if(byte != 0)
	{
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				bit++;
			}
			cout<<byte;
			//cout<<"\n";
		}
		//	return byte;
	}
		if(byte != 0)
	{
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				bit++;
			}
			cout<<byte;
			//cout<<"\n";
		}
		//	return byte;
	}
	if(byte != 0)
	{
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				bit++;
			}
			cout<<byte;
			//cout<<"\n";
		}
		//	return byte;
	cout<<"\n";
	}
}
