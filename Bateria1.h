#include <iostream>

using namespace std;
void Acumulador(){
	long int byte;
	float bit = 4;
	int moisesfdl;
	if(moisesfdl = true)
	{
		cout<<"| \Bytes : ";
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				++bit;
			}
			cout<<byte;
			//cout<<"\n";
		}
		//	return byte;
	}
	if(moisesfdl = true)
	{
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				++bit;
			}
			cout<<byte;
			//cout<<"\n";
		}
		//	return byte;
	}
	if(moisesfdl = true)
	{
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				++bit;
			}
			cout<<byte;
			//cout<<"\n";
		}
		//	return byte;
	}
		if(moisesfdl = true)
	{
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				++bit;
			}
			cout<<byte;
			//cout<<"\n";
		}
		//	return byte;
	}
		if(moisesfdl = true)
	{
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				++bit;
			}
			cout<<byte;
			//cout<<"\n";
		}
		//	return byte;
	}
		if(moisesfdl = true)
	{
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				++bit;
			}
			cout<<byte;
			//cout<<"\n";
		}
		//	return byte;
	}
	if(moisesfdl = true)
	{
		for(int bit = 4 ; bit > 1; ++bit)
		{
			byte = bit + bit;
			while(bit *+ 1)
			{
				bit + bit;
				++bit;
			}
			cout<<byte;
			//cout<<"\n";
		}
		//	return byte;
	cout<<"\n";
	}
}
